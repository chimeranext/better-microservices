# Cloud (Kubeflow/k8s) deployment of vision-core's gRPC + REST API.
# Batch crop-health sweeps + authoritative RF-DETR-Seg path. GPU-scheduled.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vision-core
  namespace: vision-core
  labels:
    app.kubernetes.io/name: vision-core
    app.kubernetes.io/part-of: better-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: vision-core
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vision-core
    spec:
      containers:
        - name: vision-core
          image: ghcr.io/chimeranext/vision-core:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: VISION_TIER
              value: "cloud"
            - name: VISION_DEFAULT_FAMILY
              value: "rfdetr_seg"      # Apache-2.0; cloud accuracy default
            - name: VISION_INFERENCE_LOCAL
              value: "false"
            # Tier-1 detection (Triton) InferenceService.
            - name: VISION_KSERVE_URL
              value: "http://vision-core-triton-detection.vision-core.svc.cluster.local"
            # Tier-1 diagnosis (vLLM) InferenceService — always cloud.
            - name: VISION_VLLM_URL
              value: "http://vision-core-vllm-diagnosis.vision-core.svc.cluster.local"
            # MinIO object storage (s3fs data backbone).
            - name: VISION_MINIO_ENDPOINT
              value: "http://minio.vision-core.svc:9000"
            # EMQX capture-ingest bridge (Vertivo orchestrator contract).
            - name: VISION_MQTT_BROKER_URL
              value: "mqtt://emqx-listeners:1883"
          ports:
            - name: grpc
              containerPort: 50051
            - name: http
              containerPort: 8080
            - name: metrics
              containerPort: 9090
          resources:
            limits:
              nvidia.com/gpu: "1"
            requests:
              cpu: "500m"
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: vision-core
  namespace: vision-core
spec:
  selector:
    app.kubernetes.io/name: vision-core
  ports:
    - name: grpc
      port: 50051
      targetPort: 50051
    - name: http
      port: 8080
      targetPort: 8080

{
  "name": "better-microservices",
  "version": "0.0.0",
  "private": true,
  "description": "Source-available microservices monorepo — pick the services your startup needs.",
  "repository": {
    "type": "git",
    "url": "https://github.com/chimeranext/better-microservices.git"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
